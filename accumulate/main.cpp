#include <iostream>
#include <thread>
#include <vector>
#include <algorithm>
#include <chrono>


template <typename It, typename T>
T parallel_accumulate(It first, It last, T init)
{
    // TODO parallel version of accumulate
}

int main(int argc, char *argv[])
{
 
}
